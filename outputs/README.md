# Outputs / 运行输出

该目录用于程序自动生成的结果，例如：

```text
outputs/
├── indices/
├── water_masks/
├── vectors/
├── dataset_split/
├── runs/
├── evaluation/
├── figures/
└── tables/
```

除 `.gitkeep` 和本说明外，输出文件默认不提交。
